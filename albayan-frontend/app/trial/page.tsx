import { TrialPlayer } from "@/features/trial/components/trial-player";

/**
 * النسخة التجريبية — درس مصغّر للزائر (فقرة + فيديو قصير + سؤالين)
 * قبل التسجيل. يستهلك نفس محرك الدرس (LessonShell/Stages).
 */
export default function TrialPage() {
  return <TrialPlayer />;
}
